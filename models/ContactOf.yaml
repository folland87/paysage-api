title: ContactOf
type: object
x-tags:
  - Common
properties:
  id:
    type: string
    description: Unique identifier.
    readOnly: true
  contactType:
    type: string
    minLength: 1
  contact:
    $ref: ./Person.yaml
  contactOf:
    type: string
    description: Paysage object that owns the contact.
    readOnly: true
  contactId:
    type: string
    writeOnly: true
  email:
    type: string
    format: email
required:
  - contactType
  - contact
  - contactId
